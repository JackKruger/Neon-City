{
  "name": "neon-bay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test:map": "node --test scripts/map/*.test.mjs",
    "map:build": "node scripts/build-map.mjs --open-data",
    "map:download": "node scripts/build-map.mjs --download-open-data",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "csv-parse": "^7.0.1",
    "geotiff": "^3.0.5",
    "proj4": "^2.20.9",
    "shapefile": "^0.6.6",
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}
